import { formatFloat } from 'crm-api/helpers/format-float';
import { module, test } from 'qunit';

module('Unit | Helper | format float');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = formatFloat([42]);
  assert.ok(result);
});
